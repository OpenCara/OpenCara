{
  "name": "@opencara/worker",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --build",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@opencara/shared": "workspace:*",
    "@supabase/supabase-js": "^2.99.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250214.0",
    "wrangler": "^4.0.0"
  }
}
